version: 1.0
runtime: python3 
build:
  commands:
    build:
      - ls
      - python -m venv venv
      - ls
      - venv\Scripts\Activate
      - pip install -r requirements.txt
    post-build:
      - python manage.py test
