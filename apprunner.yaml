version: 1.0
runtime: python3 
build:
  commands:
    build:        
      - python -m venv venv
      - venv\Scripts\Activate
      - pip install -r requirements.txt
    post-build:
      - python manage.py test
